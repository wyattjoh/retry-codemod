{
  "name": "retry-codemod",
  "module": "index.ts",
  "scripts": {
    "build": "esbuild ./src/*.ts --bundle --platform=node --format=cjs --outdir=./dist"
  },
  "devDependencies": {
    "@types/jscodeshift": "^0.11.11",
    "esbuild": "^0.21.2",
    "jscodeshift": "^0.15.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "picocolors": "^1.0.1"
  }
}
